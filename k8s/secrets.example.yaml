# Origen: plantilla/microservicio/esqueleto/k8s/secrets.example.yaml
apiVersion: v1
kind: Secret
metadata:
  name: como-estoy-hecho-secrets
type: Opaque
stringData:
  # Los VALORES los inyecta el banco (K8s Secret / Vault)
  # Aquí solo los NOMBRES de las variables
  SERVICE_NAME: "como-estoy-hecho"
  SERVICE_PORT: "8010"
  # ANTHROPIC_API_KEY: ""
  # OPENAI_API_KEY: ""
  # DATABASE_URL: ""
